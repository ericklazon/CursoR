#Usamos help para poder conocer los parametros de la funcion
help(sd)
######sd(x, na.rm = FALSE)
###Arguments
#x	a numeric vector or an R object which is coercible to one by as.double(x).
#na.rm logical. Should missing values be removed?
sd(0:100)